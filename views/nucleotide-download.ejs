<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NCBI Nucleotide Sequence Download - Jyotsna's NCBI Tools</title>
    <link rel="stylesheet" href="/styles/nucleotide-download.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/header') %>

    <div class="container mt-5">
        <h1>Nucleotide Sequence Downloader</h1>
        
        <div class="card">
            <div class="card-body">
                <form id="download-form">
                    <div class="form-group">
                        <label for="sequenceIds">Enter Sequence IDs</label>
                        <textarea 
                            id="sequenceIds" 
                            class="form-control" 
                            rows="5" 
                            placeholder="Enter sequence IDs (one per line or comma-separated)&#10;Example: MN223758, MN223759"
                            required></textarea>
                        <small class="form-text text-muted">
                            Enter multiple sequence IDs separated by commas or newlines.
                        </small>
                    </div>
                    
                    <button type="submit" id="downloadBtn" class="btn btn-primary">
                        Download Sequences
                    </button>
                </form>
                
                <div id="status" class="alert mt-3"></div>
                <div id="progress" class="progress-bar mt-3"></div>
                <div id="result" class="mt-4"></div>
            </div>
        </div>
    </div>

    <style>
    .error {
        color: #dc3545;
        margin: 5px 0;
    }

    .info {
        color: #0d6efd;
    }

    .sequence-result {
        margin-top: 15px;
        padding: 15px;
        background-color: #f8f9fa;
        border-radius: 4px;
        white-space: pre-wrap;
        word-wrap: break-word;
        max-height: 400px;
        overflow-y: auto;
    }

    .progress-bar {
        height: 20px;
        background-color: #e9ecef;
        border-radius: 4px;
        padding: 5px;
        text-align: center;
        font-size: 12px;
    }
    </style>

    <%- include('partials/footer') %>
</body>
</html>